# دليل تكامل واختبار نظام التعرف على الوجوه للفنادق

## 1. مقدمة

هذا الدليل يوفر إرشادات شاملة لتكامل واختبار نظام التعرف على الوجوه للفنادق. يغطي الدليل خطوات التثبيت، والتكوين، والاختبار لضمان عمل النظام بشكل صحيح.

## 2. متطلبات النظام

### 2.1 متطلبات الخادم
- نظام تشغيل: Ubuntu 20.04 أو أحدث
- Python 3.8 أو أحدث
- PostgreSQL 12 أو أحدث (اختياري، يمكن استخدام SQLite للبيئات الصغيرة)
- وحدة معالجة مركزية: 4 أنوية على الأقل
- ذاكرة: 8 جيجابايت على الأقل
- مساحة تخزين: 20 جيجابايت على الأقل

### 2.2 متطلبات العميل
- متصفح حديث (Chrome، Firefox، Edge، Safari)
- كاميرا ويب بدقة 720p على الأقل
- اتصال إنترنت مستقر

## 3. تثبيت وتكوين الخادم

### 3.1 تثبيت المتطلبات الأساسية

```bash
# تثبيت المتطلبات الأساسية
sudo apt update
sudo apt install -y python3-pip python3-dev build-essential libssl-dev libffi-dev python3-setuptools python3-venv

# إنشاء بيئة افتراضية
python3 -m venv venv
source venv/bin/activate

# تثبيت المكتبات المطلوبة
pip install -r requirements.txt
```

### 3.2 تكوين قاعدة البيانات

```bash
# إنشاء جداول قاعدة البيانات
python -c "from app.database import create_tables; create_tables()"
```

### 3.3 تشغيل الخادم

```bash
# تشغيل الخادم
uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

## 4. تثبيت وتكوين العميل

### 4.1 تثبيت المتطلبات الأساسية

```bash
# تثبيت Node.js و npm
sudo apt update
sudo apt install -y nodejs npm

# تثبيت المكتبات المطلوبة
cd frontend
npm install
```

### 4.2 تكوين العميل

قم بإنشاء ملف `.env` في مجلد `frontend` وأضف الإعدادات التالية:

```
REACT_APP_API_BASE_URL=http://localhost:8000
```

### 4.3 تشغيل العميل

```bash
# تشغيل العميل في وضع التطوير
npm start

# أو بناء الإصدار النهائي
npm run build
```

## 5. اختبار النظام

### 5.1 اختبار تسجيل المستخدم

1. افتح متصفح الويب وانتقل إلى `http://localhost:3000/register`
2. اختر نوع المستخدم (ضيف، موظف، VIP)
3. اتبع الخطوات لالتقاط صورة الوجه وإدخال معلومات المستخدم
4. تحقق من نجاح عملية التسجيل
5. تحقق من وجود بيانات المستخدم في قاعدة البيانات

### 5.2 اختبار التعرف على الوجه

1. افتح متصفح الويب وانتقل إلى `http://localhost:3000/verify`
2. قم بالتقاط صورة للوجه المسجل مسبقًا
3. تحقق من نجاح عملية التعرف على الوجه
4. تحقق من ظهور معلومات المستخدم الصحيحة

### 5.3 اختبار التعرف على وجوه متعددة

1. افتح متصفح الويب وانتقل إلى `http://localhost:3000/verify-multiple`
2. قم بالتقاط صورة تحتوي على وجوه متعددة (بعضها مسجل وبعضها غير مسجل)
3. تحقق من نجاح عملية التعرف على الوجوه المسجلة
4. تحقق من تحديد الوجوه غير المسجلة بشكل صحيح

### 5.4 اختبار نظام الإنذارات الصوتية

1. افتح متصفح الويب وانتقل إلى `http://localhost:3000/admin/alerts`
2. قم بتكوين إعدادات الإنذارات الصوتية
3. قم بتشغيل اختبار لكل نوع من أنواع الإنذارات
4. تحقق من تشغيل الصوت المناسب لكل حالة

### 5.5 اختبار لوحة التحكم

1. افتح متصفح الويب وانتقل إلى `http://localhost:3000/admin/dashboard`
2. تحقق من عرض الإحصائيات بشكل صحيح
3. قم بتصفية البيانات حسب التاريخ والموقع
4. تحقق من عرض المخططات والرسوم البيانية بشكل صحيح
5. تحقق من عرض سجلات الوصول بشكل صحيح

## 6. اختبار الأداء

### 6.1 اختبار الأداء الأساسي

1. قم بقياس وقت الاستجابة لعمليات التعرف على الوجه
2. تحقق من أن وقت الاستجابة أقل من 2 ثانية لعملية التعرف على وجه واحد
3. تحقق من أن وقت الاستجابة أقل من 5 ثوانٍ لعملية التعرف على وجوه متعددة

### 6.2 اختبار التحميل

1. قم بمحاكاة 10 مستخدمين متزامنين يقومون بعمليات التعرف على الوجه
2. تحقق من استقرار النظام وعدم حدوث أخطاء
3. قم بقياس استخدام الموارد (وحدة المعالجة المركزية، الذاكرة) أثناء التحميل

### 6.3 اختبار الاستقرار

1. قم بتشغيل النظام لمدة 24 ساعة متواصلة
2. قم بإجراء عمليات التعرف على الوجه بشكل دوري
3. تحقق من عدم حدوث تسرب للذاكرة أو تدهور في الأداء

## 7. استكشاف الأخطاء وإصلاحها

### 7.1 مشاكل الخادم

- **خطأ في الاتصال بقاعدة البيانات**: تحقق من إعدادات الاتصال بقاعدة البيانات في ملف `database.py`
- **خطأ في تحميل نماذج التعرف على الوجه**: تحقق من تثبيت مكتبة `face_recognition` بشكل صحيح
- **بطء في عمليات التعرف على الوجه**: تحقق من توفر وحدة معالجة رسومات (GPU) وتكوينها بشكل صحيح

### 7.2 مشاكل العميل

- **خطأ في الوصول إلى الكاميرا**: تحقق من منح الإذن للمتصفح للوصول إلى الكاميرا
- **بطء في تحميل الصفحات**: تحقق من اتصال الشبكة وأداء الخادم
- **عدم تشغيل الأصوات**: تحقق من وجود ملفات الصوت في المجلد الصحيح وإعدادات الصوت في المتصفح

## 8. الخاتمة

بعد اتباع هذا الدليل، يجب أن يكون نظام التعرف على الوجوه للفنادق مثبتًا ومكونًا بشكل صحيح ويعمل بكفاءة. إذا واجهت أي مشاكل، فيرجى الرجوع إلى قسم استكشاف الأخطاء وإصلاحها أو الاتصال بفريق الدعم.
